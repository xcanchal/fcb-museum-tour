module.exports = {
  languages: ['CA', 'EN', 'ES', 'FR'],
  defaultLanguage: 'CA',
  timeout: 60, // time to return back to initial screen after any interaction, in seconds
  videoMask: false, // gradient layer over the initial screen video
  marker: 'marker.png', // file name for "you are here" marker
};
